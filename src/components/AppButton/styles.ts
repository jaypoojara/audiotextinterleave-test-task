import {StyleSheet} from 'react-native';
import colors from '@theme/colors';

export const styles = StyleSheet.create({
  container: {
    padding: 15,
    borderRadius: 100,
    alignItems: 'center',
  },
  btnText: {
    color: colors.languageBadgeBackground,
    fontSize: 16,
    fontWeight: '700',
  },
});
